import styled from 'styled-components';

export const StyledTitle = styled.h2`
    font-weight: lighter;
    text-align: center;

    margin-bottom: 30px;
    border-bottom: 2px solid black;

    &:hover {
        cursor: pointer;
        font-weight: normal;
    }
`;